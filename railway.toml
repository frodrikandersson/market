# Railway deployment configuration

[deploy]
# Use the health check endpoint that doesn't require database
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
